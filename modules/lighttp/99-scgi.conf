# fastscgi socket
scgi.server = (
	"/rutorrent/RPC2" => ( 
		"127.0.0.1" => (
			"socket" => "/tmp/rpc.socket",
			"check-local" => "disable",
			"disable-time" => 0,
		)
	)
)

# SSL
ssl.engine  = "enable"
ssl.pemfile = "/etc/lighttpd/ssl/server.pem"

